{
  "name": "tumblrsync",
  "version": "1.0.0",
  "description": "Keeps a local Tumblr copy of the blog",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "build": "tsc --build --force --verbose",
    "prestart": "tsc --build --force --verbose",
    "start": "node --env-file=.env dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/lodash-es": "^4.17.12",
    "@types/node": "^22.13.0",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "lodash-es": "^4.17.21"
  }
}
